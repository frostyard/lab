apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: hive
resources:
  - deployment.yaml
  - service.yaml
  - network-policy.yaml
configMapGenerator:
  - name: hive-observer-code
    files:
      - observer.py
