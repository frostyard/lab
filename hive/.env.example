# Copy to .env (chmod 600) next to docker-compose.yaml.
# Bearer token required on all /api/* dashboard endpoints (except /api/health).
# Any long random string works: openssl rand -hex 32
HIVE_DASHBOARD_TOKEN=
